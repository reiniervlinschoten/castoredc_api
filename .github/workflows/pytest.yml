name: pytest

'on':
  push:
    branches:
      - '**'
    tags:
      - '**'
  pull_request:
    branches:
      - '**'
    types: [opened]
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v1
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov csv-diff
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Running Client Tests
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        CLIENT_STUDY_ID: ${{ secrets.CLIENT_STUDY_ID }}
        IMPORT_STUDY_ID: ${{ secrets.IMPORT_STUDY_ID }}
        STUDY_STUDY_ID: ${{ secrets.STUDY_STUDY_ID }}
        SPECIAL_STUDY_ID: ${{ secrets.SPECIAL_STUDY_ID }}
      run: |
        cd castoredc_api
        pytest test_api_endpoints --cov=./ --cov-report=xml

    - name: Running Object Tests
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        CLIENT_STUDY_ID: ${{ secrets.CLIENT_STUDY_ID }}
        IMPORT_STUDY_ID: ${{ secrets.IMPORT_STUDY_ID }}
        STUDY_STUDY_ID: ${{ secrets.STUDY_STUDY_ID }}
        SPECIAL_STUDY_ID: ${{ secrets.SPECIAL_STUDY_ID }}
      run: |
        cd castoredc_api
        pytest test_castor_objects --cov=./ --cov-append

    - name: Running Integration Tests
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        CLIENT_STUDY_ID: ${{ secrets.CLIENT_STUDY_ID }}
        IMPORT_STUDY_ID: ${{ secrets.IMPORT_STUDY_ID }}
        STUDY_STUDY_ID: ${{ secrets.STUDY_STUDY_ID }}
        SPECIAL_STUDY_ID: ${{ secrets.SPECIAL_STUDY_ID }}
      run: |
        cd castoredc_api
        pytest test_integration --cov=./ --cov-append

    - name: Running Output Tests
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        CLIENT_STUDY_ID: ${{ secrets.CLIENT_STUDY_ID }}
        IMPORT_STUDY_ID: ${{ secrets.IMPORT_STUDY_ID }}
        STUDY_STUDY_ID: ${{ secrets.STUDY_STUDY_ID }}
        SPECIAL_STUDY_ID: ${{ secrets.SPECIAL_STUDY_ID }}
      run: |
        cd castoredc_api
        pytest test_output --cov=./ --cov-append

    - name: Running Import Tests
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        CLIENT_STUDY_ID: ${{ secrets.CLIENT_STUDY_ID }}
        IMPORT_STUDY_ID: ${{ secrets.IMPORT_STUDY_ID }}
        STUDY_STUDY_ID: ${{ secrets.STUDY_STUDY_ID }}
        SPECIAL_STUDY_ID: ${{ secrets.SPECIAL_STUDY_ID }}
      run: |
        cd castoredc_api
        pytest test_import --cov=./ --cov-append

    - name: "Upload coverage to Codecov"
      uses: codecov/codecov-action@v2
      with:
        fail_ci_if_error: true
        token: ${{ secrets.CODECOV_TOKEN }}
